---
- hosts: all
  become: true
  tasks:
  - name: Install httpd
    yum:
     name: httpd
     state: installed
  - name: Activate httpd
    service:
      name: httpd
      state: started
      enabled: yes
  - name: Create file /etc/ansible_abort.txt
    copy:
     content: my is also node {{ansible_fqdn}}
     mode: 0644
     force: no
     dest: /etc/ansible_abort.txt
